// Removed feature: engagement audit service.
// Stub retained so legacy imports do not break the build.
export const engagementAuditService = {
  list: async () => [] as unknown[],
  add: async (_entry: unknown) => {},
};

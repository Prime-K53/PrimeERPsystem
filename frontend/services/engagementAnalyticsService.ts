// Removed feature: engagement analytics service.
// Stub retained so legacy imports do not break the build.
export const engagementAnalyticsService = {
  compute: async () => ({}),
  getSummary: async () => ({}),
};

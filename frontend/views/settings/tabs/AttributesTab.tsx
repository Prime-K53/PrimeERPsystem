// Removed feature: product attributes tab.
// Stub retained so legacy settings tabs do not break the build.
import React from 'react';
const AttributesTab: React.FC = () => null;
export default AttributesTab;

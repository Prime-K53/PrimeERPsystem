# Migration Exception Register — Phase 2.6 Accounting Closure

**Project:** Prime ERP
**Register date:** 2026-09-04
**Database:** Supabase project `rdtuzuzehfbwvfdzqliw` (company TESTING/STAGING project, treated as live)
**Verifier:** `backend/scripts/phase26Closure.cjs` (read-only; nothing in this register was modified)

This register is the permanent record of controlled historical exceptions from the
Chart-of-Accounts (COA) migration and legacy ledger migration. The original malformed
evidence is preserved — **no automatic repair, no correcting journals, no rewriting of
invoice history** has been performed or is authorized by this register.

---

## 1. Historical ACC-* ledger exception

```text
Exception:
ACC-1786942904915-jpgnq

Affected invoices:
24

Malformed ledger entries:
16

Canonical existing postings:
3

Invoices without ledger entries:
4  (INV-P726/036, INV-P726/039, INV-P726/040, INV-P726/043)

Original account:
Unrecoverable

Root cause:
Ephemeral account ID generated by legacy Chart of Accounts UI,
followed by invoice posting without canonical account resolution,
followed by deletion of the account.

Current vulnerability:
Fixed

Automatic repair:
Not authorized

Candidate account:
41100 Product Sales

Candidate confidence:
UNKNOWN

Production data modified:
NO
```

### Verified state (2026-09-04)

- Occurrences of `ACC-1786942904915-jpgnq`: **40** — 16 malformed ledger entries
  (reference `LG-INV-AR-*` rows, still old-format, no canonical `account_id`/`entry_type`,
  original invoice references intact, customer references intact) + 24 invoice references.
- The 16 malformed entries map to 16 distinct invoices:
  `INV-P726/003, 004, 005, 006, 007, 008, 010, 028, 030, 031, 032, 033, 034, 035, 037, 038`.
- The 16 entries are excluded from canonical reporting (they lack canonical fields).
- Re-running the corrected migration (`migrateLegacyLedger.cjs --dry-run`):
  `Would change=0, Skipped=16, Errors=0` — migration is idempotent, no duplicates possible.

### Related invoices

| Invoice | Status | Ledger state |
|---|---|---|
| INV-0002 | Partial | Two migrated MIGR- pairs (80,600 + 403,000 = 483,600). Invoice total is 403,000 → suspected duplication, **report for controlled remediation** |
| INV-P726/009 | Unpaid | Single MIGR- pair 175,000 = invoice total ✓ |
| INV-P726/029 | Unpaid | Single MIGR- pair 85,000 = invoice total ✓ |
| INV-P726/041 | Unpaid | Single MIGR- pair 79,400 = invoice total ✓ (listed as "no ledger" in the phase 2.6 brief; a canonical posting exists) |
| INV-P726/036, 039, 040 | Unpaid | No ledger entries — historically missing AR postings (legacy invoice-posting path never wrote the ledger) |
| INV-P726/043 | Cancelled | No ledger entries — legitimately never posted |

---

## 2. Additional findings from closure verification (2026-09-04)

These were discovered by the Phase 2.6 closure scan. None have been modified.
They are recorded here for controlled remediation (separate work items, not automatic).

### 2.1 Zero-UUID test rows (2 ledger entries) — active invalid GL references

```text
Ledger entries: 9e8a7aac-18c0-49b1-94f3-8130f1273fd8, ba5a0897-1dc3-4327-83dc-d771c729e751
account_id:     00000000-0000-0000-0000-000000000000 (no matching COA row)
description:    "Should fail"
created_by:     test-user-001
entry_date:     2026-07-15
amount:         100 each (debit), balanced within their journals (credit leg to 11110)
```

Evidence of the pre-fix vulnerability (invalid account IDs could be persisted); test
artifacts, not business postings. They violate "every canonical posting resolves to a COA
account" and "active invalid GL references = 0". **Do not auto-fix** — controlled removal
should be agreed and executed as a separate, reviewed operation.

### 2.2 Phase 2.5 acceptance-test residue in the live ledger (100 ledger rows)

```text
Rows:        100 active PH25-TEST-* ledger rows (reference/created_by/description)
Amount:      Dr = Cr = 49,250 (fully balanced, valid canonical UUID accounts)
Cause:       phase25_accounting_acceptance.cjs cleanup filters ledger rows by id prefix,
             but expense/income/transfer ledger rows carry UUID ids — cleanup cannot
             match them, so every acceptance run accumulates rows in the live ledger.
```

The residue is balanced and canonical (it does not corrupt), but it inflates Trial
Balance / P&L / Balance Sheet figures and explains the Trial Balance drift:

```text
Expected (Phase 2.6 brief): Dr = Cr = 40,237,799
Measured (2026-09-03 16:43, before this closure run): Dr = Cr = 40,242,549
Measured (after this closure's acceptance re-run):      Dr = Cr = 40,247,299
Delta per acceptance run: 4,750
```

### 2.3 Sign semantics of migrated equity account 34000 (Drawings)

```text
Ledger balance: 34000 Drawings (EQUITY/DEBIT) carries a net CREDIT of 185,500.
Cause: legacy code '3000' was credited like an equity-capital account in old postings
       and migrated to 34000 Drawings (debit-normal) with side preserved.
Impact: Balance Sheet equation holds when 34000 is treated with contra-equity semantics
       (Assets = Liabilities + Equity + Revenue − Expenses, residual 200 = the 2.1 rows).
       The '3000' → '34000' mapping is a candidate semantic mapping issue for review.
```

---

## 3. Controlled remediation backlog (proposed, NOT executed)

1. Remove the 2 zero-UUID "Should fail" test rows (2.1) — requires the paired credit legs
   to be handled as one journal each (balanced), after sign-off.
2. Remove or quarantine the 100 PH25-TEST-* ledger rows (2.2) and fix the acceptance-test
   cleanup so future runs cannot accumulate rows.
3. Investigate the INV-0002 double posting (section 1) — independent accounting review
   before any adjustment; invoice total is 403,000, posted total is 483,600.
4. Review the `3000` → `34000` legacy mapping (2.3).
5. Determine the original account for `ACC-1786942904915-jpgnq` (candidate 41100, UNKNOWN
   confidence) before any corrective mapping decision.

**No item above may be executed as part of Phase 2.6 closure.**